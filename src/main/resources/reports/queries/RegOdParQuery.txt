select outs.REGION_NAME, 
OUTS.ACTIVE_CLIENTS,
P_1.REGULAR_OD_CLIENTS,
nvl(OUTS.OUTSTANDING,0) OUTSTANDING,
P_1.REGULAR_OD_AMOUNT,               
       round((NVL (p_4.REGULAR_OD_CLIENTS, 1) / nvl(p_1.REGULAR_OD_CLIENTS,0)) * 100,2) PAR_5_PERC,      
       round((NVL (p_15.REGULAR_OD_CLIENTS, 1) / nvl(p_1.REGULAR_OD_CLIENTS,0)) * 100,2) PAR_15_PERC,       
       round((NVL (p_29.REGULAR_OD_CLIENTS, 1) / nvl(p_1.REGULAR_OD_CLIENTS,0)) * 100,2) PAR_30_PERC,       
       round((NVL (p_59.REGULAR_OD_CLIENTS, 1) / nvl(p_1.REGULAR_OD_CLIENTS,0)) * 100,2) PAR_45_PERC,       
       round((NVL (p_89.REGULAR_OD_CLIENTS, 1) / nvl(p_1.REGULAR_OD_CLIENTS,0)) * 100,2) PAR_60_PERC,       
       round((NVL (p_90.REGULAR_OD_CLIENTS, 1) / nvl(p_1.REGULAR_OD_CLIENTS,0)) * 100,2) PAR_90_PERC,       
       round((NVL (p_91.REGULAR_OD_CLIENTS, 1) / nvl(p_1.REGULAR_OD_CLIENTS,0)) * 100,2) PAR_91_PERC,
       round((NVL (p_1.REGULAR_OD_CLIENTS, 1) / nvl(OUTS.ACTIVE_CLIENTS,0)) * 100,2) PAR_1_PERC        
       from
(SELECT r.REGION_NAME, 
       COUNT(LOAN_CYCLE_ID) ACTIVE_CLIENTS, 
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) OUTSTANDING
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK CPM,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr,location@LINK_TO_TESTMWC.KASHF.ORG.PK l,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa,region@LINK_TO_TESTMWC.KASHF.ORG.PK r        
WHERE (CPM.LOAN_COMPLETION_DATE IS NULL OR CPM.LOAN_COMPLETION_DATE > to_date(:to_dt,'dd-mm-yyyy')) 
AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND  (NVL(OUTSTANDING_PRINCIPAL,0) + NVL(OUTSTANDING_SC,0)) > 0
AND CPM.PRODUCT_ID = pr.PRODUCT_CODE
and CPM.BRANCH_ID = b.BRANCH_CD
and l.LOCATION_ID = b.LOCATION_ID
and l.REGION_CD = r.REGION_CD
and l.AREA_ID = aa.AREA_ID
and pr.PRODUCT_CODE not in (8)
--and aa.AREA_ID = 80
--and cpm.PRODUCT_ID = 13
GROUP BY r.REGION_NAME
)outs,
(SELECT r1.REGION_NAME, 
       COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
       FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK cpm1,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr1,location@LINK_TO_TESTMWC.KASHF.ORG.PK l1,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b1,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa1,region@LINK_TO_TESTMWC.KASHF.ORG.PK r1 
       WHERE disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) 
AND CPM1.PRODUCT_ID = pr1.PRODUCT_CODE 
and CPM1.BRANCH_ID = b1.BRANCH_CD 
and l1.LOCATION_ID = b1.LOCATION_ID 
and l1.REGION_CD = r1.REGION_CD 
and l1.AREA_ID = aa1.AREA_ID 
and pr1.PRODUCT_CODE not in (8)
--and cpm1.PRODUCT_ID = 13
--and aa1.AREA_ID = 80    
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
GROUP BY r1.REGION_NAME
)p_1, 
(SELECT r2.REGION_NAME,
       COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 0 and 3.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_4,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 3.51 and 18.50 
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_15,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 18.51 and 28.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_29,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 28.51 and 58.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_59,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 58.51 and 88.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_89,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS > 88.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_90,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS > 88.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_91
where outs.REGION_NAME = p_1.REGION_NAME(+) 
and outs.REGION_NAME = p_4.REGION_NAME(+) 
and outs.REGION_NAME = p_15.REGION_NAME(+) 
and outs.REGION_NAME = p_29.REGION_NAME(+) 
and outs.REGION_NAME = p_59.REGION_NAME(+) 
and outs.REGION_NAME = p_89.REGION_NAME(+) 
and outs.REGION_NAME = p_90.REGION_NAME(+)
and outs.REGION_NAME = p_91.REGION_NAME(+) 
union
select 'Total' Total,
sum(OUTS.ACTIVE_CLIENTS) ACTIVE_CLIENTS,
sum(P_1.REGULAR_OD_CLIENTS) REGULAR_OD_CLIENTS,
sum(nvl(OUTS.OUTSTANDING,0)) OUTSTANDING,
sum(P_1.REGULAR_OD_AMOUNT) REGULAR_OD_AMOUNT,       
       round(( sum(NVL (p_4.REGULAR_OD_CLIENTS, 1)) / sum(nvl(P_1.REGULAR_OD_CLIENTS,0))) * 100,2) PAR_5_PERC,      
       round(( sum(NVL (p_15.REGULAR_OD_CLIENTS, 1)) / sum(nvl(P_1.REGULAR_OD_CLIENTS,0))) * 100,2) PAR_15_PERC,       
       round(( sum(NVL (p_29.REGULAR_OD_CLIENTS, 1)) / sum(nvl(P_1.REGULAR_OD_CLIENTS,0))) * 100,2) PAR_30_PERC,       
       round(( sum(NVL (p_59.REGULAR_OD_CLIENTS, 1)) / sum(nvl(P_1.REGULAR_OD_CLIENTS,0))) * 100,2) PAR_45_PERC,       
       round(( sum(NVL (p_89.REGULAR_OD_CLIENTS, 1)) / sum(nvl(P_1.REGULAR_OD_CLIENTS,0))) * 100,2) PAR_60_PERC,       
       round(( sum(NVL (p_90.REGULAR_OD_CLIENTS, 1)) / sum(nvl(P_1.REGULAR_OD_CLIENTS,0))) * 100,2) PAR_90_PERC,       
       round(( sum(NVL (p_91.REGULAR_OD_CLIENTS, 1)) / sum(nvl(P_1.REGULAR_OD_CLIENTS,0))) * 100,2) PAR_91_PERC,
       round(( sum(NVL (p_1.REGULAR_OD_CLIENTS, 1)) / sum(nvl(P_1.REGULAR_OD_CLIENTS,0))) * 100,2) PAR_1_PERC      
       from
(SELECT r.REGION_NAME, 
       COUNT(LOAN_CYCLE_ID) ACTIVE_CLIENTS, 
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) OUTSTANDING
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK CPM,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr,location@LINK_TO_TESTMWC.KASHF.ORG.PK l,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa,region@LINK_TO_TESTMWC.KASHF.ORG.PK r        
WHERE (CPM.LOAN_COMPLETION_DATE IS NULL OR CPM.LOAN_COMPLETION_DATE > to_date(:to_dt,'dd-mm-yyyy')) 
AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND  (NVL(OUTSTANDING_PRINCIPAL,0) + NVL(OUTSTANDING_SC,0)) > 0
AND CPM.PRODUCT_ID = pr.PRODUCT_CODE
and CPM.BRANCH_ID = b.BRANCH_CD
and l.LOCATION_ID = b.LOCATION_ID
and l.REGION_CD = r.REGION_CD
and l.AREA_ID = aa.AREA_ID
and pr.PRODUCT_CODE not in (8)
--and aa.AREA_ID = 80
--and cpm.PRODUCT_ID = 13
GROUP BY r.REGION_NAME
)outs,
(SELECT r1.REGION_NAME, 
       COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
       FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK cpm1,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr1,location@LINK_TO_TESTMWC.KASHF.ORG.PK l1,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b1,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa1,region@LINK_TO_TESTMWC.KASHF.ORG.PK r1 
       WHERE disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) 
AND CPM1.PRODUCT_ID = pr1.PRODUCT_CODE 
and CPM1.BRANCH_ID = b1.BRANCH_CD 
and l1.LOCATION_ID = b1.LOCATION_ID 
and l1.REGION_CD = r1.REGION_CD 
and l1.AREA_ID = aa1.AREA_ID 
and pr1.PRODUCT_CODE not in (8)
--and cpm1.PRODUCT_ID = 13
--and aa1.AREA_ID = 80    
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
GROUP BY r1.REGION_NAME
)p_1, 
(SELECT r2.REGION_NAME,
       COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 0 and 3.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_4,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 3.51 and 18.50 
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_15,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 18.51 and 28.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_29,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 28.51 and 58.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_59,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS between 58.51 and 88.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_89,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS > 88.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_90,
(SELECT r2.REGION_NAME,
        COUNT(LOAN_CYCLE_ID) REGULAR_OD_CLIENTS, 
       ROUND(SUM(nvl(OVERDUE_AMOUNT,0))+SUM(nvl(OVERDUE_AMOUNT_sc,0))) REGULAR_OD_AMOUNT,
       ROUND(SUM(NVL(OUTSTANDING_PRINCIPAL,0))) + ROUND(SUM(NVL(OUTSTANDING_SC,0))) REGULAR_PAR_AMOUNT 
FROM con_mon_asofdate_rschd@LINK_TO_TESTMWC.KASHF.ORG.PK  cpm2,product@LINK_TO_TESTMWC.KASHF.ORG.PK pr2,location@LINK_TO_TESTMWC.KASHF.ORG.PK l2,branch@LINK_TO_TESTMWC.KASHF.ORG.PK b2,area@LINK_TO_TESTMWC.KASHF.ORG.PK aa2,region@LINK_TO_TESTMWC.KASHF.ORG.PK r2 
WHERE ( OVERDUE_AMOUNT>0 OR OVERDUE_AMOUNT_SC>0) AND disbursed_date between'01-may-2005' and to_date(:to_dt,'dd-mm-yyyy')
AND OVERDUE_DAYS > 88.50
AND (LOAN_COMPLETION_DATE IS NULL OR LOAN_COMPLETION_DATE >to_date(:to_dt,'dd-mm-yyyy')) 
AND CPM2.PRODUCT_ID = pr2.PRODUCT_CODE 
and CPM2.BRANCH_ID = b2.BRANCH_CD 
and l2.LOCATION_ID = b2.LOCATION_ID 
and l2.REGION_CD = r2.REGION_CD 
and l2.AREA_ID = aa2.AREA_ID 
and pr2.PRODUCT_CODE not in (8)
GROUP BY r2.REGION_NAME
)p_91
where outs.REGION_NAME = p_1.REGION_NAME(+) 
and outs.REGION_NAME = p_4.REGION_NAME(+) 
and outs.REGION_NAME = p_15.REGION_NAME(+) 
and outs.REGION_NAME = p_29.REGION_NAME(+) 
and outs.REGION_NAME = p_59.REGION_NAME(+) 
and outs.REGION_NAME = p_89.REGION_NAME(+) 
and outs.REGION_NAME = p_90.REGION_NAME(+)
and outs.REGION_NAME = p_91.REGION_NAME(+)     
order by 1

